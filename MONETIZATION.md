# 诗签 · 分层变现设计（预留，暂不启用）

> 当前阶段策略：**先免费做增长**。本文档与 `plan.js` 是为未来开闸预留的设计与计量骨架，
> 现在**不向用户收费、不设任何功能墙**。详见 [PRODUCT-REVIEW.md](PRODUCT-REVIEW.md) §5–6。

---

## 1. 为什么是「订阅 + 用量」混合模型

AI 生成有真实可变成本（每次摇签 = 一次 DeepSeek 调用）。2026 年订阅类 App 的主流是
**hybrid monetization**：一个基础订阅 + 一个用量额度 + 超额加购。纯「无限畅用」会被重度用户
拖垮毛利，纯一次性买断又无法覆盖持续的 AI 成本。诗签采用：**免费额度养习惯 → Pro 解锁体验 → 加购覆盖重度用量**。

## 2. 三档设计

| 档位 | 价格（建议） | 摇签额度 | 诗体 | 主题 | 其他 |
|---|---|---|---|---|---|
| **免费** | ¥0 | 每月 ~30 次 | 中文古体诗 | 基础 3 套 | 云同步、无广告、JSON/Markdown 导出 |
| **Pro** | ¥18/月 · ¥98/年 · 海外 $2.99/月 | 不限* | 中文古体诗 **+ 英文十四行诗** | 全部 7 套 | 更强 AI 模型、图片上传、年度诗集、情绪分析、周回顾 |
| **加购** | 按需 | +50 次/¥6 | — | — | 实体诗册印刷（按单）、额外摇签次数包 |

\* Pro「不限」实为软上限（如每日 50 次）防滥用，正常用户无感。

**核心原则（避开国内对付费墙的反感）**：
- 写日记、云同步、**数据导出永远免费** —— 不锁用户数据，建立信任。
- 只对「AI 用量」和「锦上添花的体验（主题 / 诗体 / 分析 / 印刷）」收费。
- 无广告是免费档也保留的承诺，写进卖点。

## 3. 免费额度怎么定

以单用户成本倒推：DeepSeek 一次摇签约 1–2K tokens ≈ ¥0.02。每月 30 次免费 ≈ ¥0.6 成本，
可由 Pro 用户与加购覆盖。额度用尽时**不冷冰冰地拦截**，而是提示「本月的签已摇满，明月再续，或解锁 Pro」。

## 4. 支付通道（按市场）

- **国内**：微信支付 / 支付宝（需主体资质）。Web 端可用 H5 支付或引导至小程序。
- **海外**：Stripe（Web 直接收款）或 Apple IAP（若用 Capacitor 上架）。
- 订阅状态存 Firestore `users/{uid}/billing`，由服务端校验（不可信前端）。

## 5. 计量骨架（已落地，`plan.js`）

`plan.js` 暴露 `window.PLAN`，现在所有门控**默认放行（全免费）**：

```js
window.PLAN.tier()            // 'free' | 'pro'（当前恒为 free）
window.PLAN.recordShake()    // 记录一次摇签用量（写 localStorage，供未来计量/分析）
window.PLAN.shakeCountThisMonth()
window.PLAN.canShake()       // 现在恒 true；将来按额度返回 false
window.PLAN.LIMITS           // { free: { shakesPerMonth }, pro: {...} } 配置
```

未来开闸只需两步：① 在 `canShake()` 接入真实额度判断；② 在摇签入口检查 `canShake()`，
为 false 时弹付费引导。当前调用点（`doShake` 成功后 `recordShake()`）已埋好，不影响现有体验。

## 6. 上线节奏（与路线图对齐）

1. **现在**：免费、埋计量点、攒用户与口碑。
2. **第二阶段**：上线英文十四行诗、周回顾、情绪分析等「Pro 候选」功能，但仍全部免费，观察哪些最被珍惜。
3. **第三阶段**：接支付，把第二阶段验证过最受欢迎的能力划入 Pro，开闸收费。

> 决策依据来自真实数据（计量点 + Vercel Analytics），而非拍脑袋。
